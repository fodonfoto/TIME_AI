name: minimal-test
description: Minimal test workflow

blocks:
  start:
    type: generic_webhook
    name: "Start Webhook"
    connections:
      success: respond

  respond:
    type: response
    name: "Simple Response"
    inputs:
      status: 200
      body:
        message: "Hello from Sim Studio!"
        timestamp: "{{now}}"